# =============================================
# Tokko— Online Shop UMKM MVP
# =============================================

# Backend (artifacts/api-server)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/online_shop_umkm"
SESSION_SECRET="change-me-to-a-random-secret"
PORT=5000

# CORS — comma-separated allowed origins
CORS_ORIGIN="http://localhost:3000"

# Frontend Shop (artifacts/shop)
# In Replit, BASE_URL is injected automatically by the artifact system
VITE_API_URL="/api"
